import { defineConfig } from 'astro/config';

export default defineConfig({
  // Set this to your Netlify URL (or custom domain) once deployed.
  // It's used for canonical URLs and the RSS-ready future.
  site: 'https://michaelcolonna.com',
});
